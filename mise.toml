# CI とローカルで同じツールを使うための pin。
#
# go は go.mod の toolchain と一致していなければならない。golangci-lint は
# 自分より新しい go directive を持つモジュールのロードを拒むので、ここと
# go.mod がずれると lint が不可解に落ちる。test_all.sh の "toolchain" が
# その一致を検査する。
[tools]
go = "1.27.0"
golangci-lint = "2.13.1"
