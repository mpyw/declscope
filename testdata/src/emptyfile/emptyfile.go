package emptyfile

// doc.go holds a package comment and nothing else, so it is not a second unit.
// The naming rule stays out, and this name is left alone.
func seed() int { return 1 }

var _ = seed
