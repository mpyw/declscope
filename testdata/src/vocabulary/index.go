package vocabulary

// The irregular plural, listed as vocabulary because no generated inflection
// produces it. The right edge stays free, so the word may end inside the name.
func indicesSorted() int { return 5 }

var _ = indicesSorted
