# defaults.unexported must reach members as well as package-level declarations,
# so that a defaults key governs every declaration in a package rather than half
# of them.
defaults:
  unexported: package
rules:
  naming:
    qualify: never
