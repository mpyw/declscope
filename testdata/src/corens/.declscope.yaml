# The core exists to be left alone by the naming rules, so both are on their
# strictest setting: whatever the core is spared here, it is spared everywhere.
rules:
  qualify: always
  unqualify: true
  exportedLabels: true
