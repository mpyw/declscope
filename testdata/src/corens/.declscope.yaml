# The core exists to be left alone by the naming rule, so it is on its
# strictest setting: whatever the core is spared here, it is spared everywhere.
rules:
  naming:
    qualify: always
    exported: true
